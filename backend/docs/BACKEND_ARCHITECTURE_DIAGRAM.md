# 🏗️ Backend Architecture Diagram

## 📐 Kiến Trúc Tổng Thể Backend

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         BACKEND SERVER                                    │
│                      (Python/FastAPI/Uvicorn)                            │
│                                                                           │
│  ┌───────────────────────────────────────────────────────────────────┐ │
│  │                    FastAPI Application Layer                        │ │
│  │                         (app/main.py)                               │ │
│  │                                                                      │ │
│  │  ┌──────────────────────────────────────────────────────────────┐ │ │
│  │  │  CORS Middleware                                              │ │ │
│  │  │  - Allow all origins                                          │ │ │
│  │  │  - Enable credentials                                         │ │ │
│  │  └──────────────────────────────────────────────────────────────┘ │ │
│  │                                                                      │ │
│  │  ┌──────────────────────────────────────────────────────────────┐ │ │
│  │  │  API Router (app/api/routes.py)                              │ │ │
│  │  │                                                               │ │ │
│  │  │  Endpoints:                                                   │ │ │
│  │  │  ├─ GET  /          → API info                               │ │ │
│  │  │  ├─ POST /learn     → Add song to database                   │ │ │
│  │  │  ├─ POST /recognize → Recognize song from audio              │ │ │
│  │  │  ├─ GET  /stats     → Database statistics                    │ │ │
│  │  │  ├─ GET  /songs     → List all songs                         │ │ │
│  │  │  ├─ DELETE /songs/{name} → Delete specific song              │ │ │
│  │  │  └─ DELETE /songs   → Clear all songs                        │ │ │
│  │  └──────────────────────────────────────────────────────────────┘ │ │
│  └───────────────────────────────────────────────────────────────────┘ │
│                                                                           │
│  ┌───────────────────────────────────────────────────────────────────┐ │
│  │                    Core Components                                 │ │
│  │                                                                      │ │
│  │  ┌──────────────────────────────────────────────────────────────┐ │ │
│  │  │  DSP Engine (app/core/dsp_engine.py)                         │ │ │
│  │  │                                                               │ │ │
│  │  │  AudioFingerprinter Class:                                    │ │ │
│  │  │  ├─ load_audio()          → Load & preprocess audio           │ │ │
│  │  │  ├─ _compute_spectrogram() → STFT transformation              │ │ │
│  │  │  ├─ _find_peaks()         → Peak detection                   │ │ │
│  │  │  ├─ generate_fingerprints() → Create hash fingerprints       │ │ │
│  │  │  └─ process_file()        → Full pipeline                     │ │ │
│  │  │                                                               │ │ │
│  │  │  Algorithm:                                                  │ │ │
│  │  │  Audio → Spectrogram → Peaks → Fingerprints                 │ │ │
│  │  └──────────────────────────────────────────────────────────────┘ │ │
│  │                                                                      │ │
│  │  ┌──────────────────────────────────────────────────────────────┐ │ │
│  │  │  Database (app/core/database.py)                              │ │ │
│  │  │                                                               │ │ │
│  │  │  PersistentDB Class:                                          │ │ │
│  │  │  ├─ add_song()        → Add song & fingerprints              │ │ │
│  │  │  ├─ query()           → Query matching song                  │ │ │
│  │  │  ├─ delete_song()     → Delete song                          │ │ │
│  │  │  ├─ get_song_count()  → Count songs                         │ │ │
│  │  │  ├─ get_fingerprint_count() → Count fingerprints             │ │ │
│  │  │  └─ list_songs()       → List all songs                      │ │ │
│  │  │                                                               │ │ │
│  │  │  Storage: SQLite (music_recognition.db)                      │ │ │
│  │  └──────────────────────────────────────────────────────────────┘ │ │
│  └───────────────────────────────────────────────────────────────────┘ │
│                                                                           │
└─────────────────────────────────────────────────────────────────────────┘
         │
         │ SQLite Connection
         ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                    SQLite Database                                       │
│                  (music_recognition.db)                                  │
│                                                                           │
│  ┌──────────────────┐          ┌──────────────────┐                     │
│  │   songs table    │          │ fingerprints     │                     │
│  │                  │          │     table        │                     │
│  │  - id (PK)       │◄─────────│  - id (PK)       │                     │
│  │  - name (UNIQUE) │          │  - hash_token    │                     │
│  │  - created_at    │          │  - song_id (FK)   │                     │
│  └──────────────────┘          │  - absolute_time │                     │
│                                 └──────────────────┘                     │
│                                                                           │
│  Indexes:                                                                 │
│  - idx_hash_token (on fingerprints.hash_token)                            │
│  - idx_song_id (on fingerprints.song_id)                                 │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Component Interaction Flow

### 1. Learn Song Flow

```
┌─────────────┐
│   Client    │
│  (Mobile/   │
│  Desktop)   │
└──────┬──────┘
       │
       │ POST /learn
       │ multipart/form-data
       │ - file: audio_file
       │ - song_name: "Song Name"
       │
       ▼
┌─────────────────────────────────────────────────────────┐
│  FastAPI Router (routes.py)                             │
│                                                          │
│  1. Validate file type                                 │
│  2. Save to temp file                                   │
│  3. Call fingerprinter.process_file()                   │
│  4. Call db.add_song()                                  │
│  5. Return JSON response                                │
└──────┬──────────────────────────────────────────────────┘
       │
       ├─────────────────────────┬──────────────────────┐
       │                         │                      │
       ▼                         ▼                      ▼
┌──────────────┐      ┌──────────────┐      ┌──────────────┐
│ DSP Engine   │      │  Database     │      │ Temp File    │
│              │      │               │      │              │
│ 1. Load      │      │ 1. Insert     │      │ (Cleanup)    │
│ 2. Process   │      │    song      │      │              │
│ 3. Generate  │      │ 2. Insert     │      │              │
│    fingerprints│      │    fingerprints│      │              │
└──────────────┘      └──────────────┘      └──────────────┘
```

### 2. Recognize Song Flow

```
┌─────────────┐
│   Client    │
│  (Mobile/   │
│  Desktop)   │
└──────┬──────┘
       │
       │ POST /recognize
       │ multipart/form-data
       │ - file: audio_sample
       │
       ▼
┌─────────────────────────────────────────────────────────┐
│  FastAPI Router (routes.py)                             │
│                                                          │
│  1. Validate file type                                  │
│  2. Check database not empty                            │
│  3. Save to temp file                                   │
│  4. Call fingerprinter.process_file()                   │
│  5. Call db.query()                                     │
│  6. Return JSON response                                │
└──────┬──────────────────────────────────────────────────┘
       │
       ├─────────────────────────┬──────────────────────┐
       │                         │                      │
       ▼                         ▼                      ▼
┌──────────────┐      ┌──────────────┐      ┌──────────────┐
│ DSP Engine   │      │  Database    │      │ Temp File    │
│              │      │              │      │              │
│ 1. Load      │      │ 1. Lookup    │      │ (Cleanup)    │
│ 2. Process   │      │    hashes    │      │              │
│ 3. Generate  │      │ 2. Calculate│      │              │
│    fingerprints│      │    offsets   │      │              │
│              │      │ 3. Time      │      │              │
│              │      │    coherency │      │              │
│              │      │ 4. Find best │      │              │
│              │      │    match     │      │              │
└──────────────┘      └──────────────┘      └──────────────┘
```

---

## 🧩 Class Diagram

```
┌─────────────────────────────────────────────────────────────┐
│                    AudioFingerprinter                       │
│                  (app/core/dsp_engine.py)                   │
├─────────────────────────────────────────────────────────────┤
│ - sample_rate: int = 22050                                   │
│ - n_fft: int = 4096                                         │
│ - hop_length: int = 1024                                    │
│ - peak_neighborhood_size: int = 20                          │
│ - target_zone_t_min: int = 1                                │
│ - target_zone_t_max: int = 5                                │
├─────────────────────────────────────────────────────────────┤
│ + load_audio(file_path: str) → np.ndarray                   │
│ + process_file(file_path: str) → List[Tuple]                │
│ - _compute_spectrogram(audio: np.ndarray) → np.ndarray      │
│ - _find_peaks(spectrogram: np.ndarray) → List[Tuple]         │
│ - generate_fingerprints(peaks: List[Tuple]) → List[Tuple]   │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                      PersistentDB                           │
│                  (app/core/database.py)                     │
├─────────────────────────────────────────────────────────────┤
│ - db_path: str                                              │
│ - conn: sqlite3.Connection                                  │
├─────────────────────────────────────────────────────────────┤
│ + __init__(db_path: str)                                    │
│ + add_song(song_name: str, fingerprints: List) → int       │
│ + query(query_fingerprints: List, min_matches: int)         │
│   → Optional[Tuple[str, int, float]]                        │
│ + delete_song(song_name: str) → Tuple[bool, int]            │
│ + get_song_count() → int                                    │
│ + get_fingerprint_count() → int                             │
│ + list_songs() → List[str]                                  │
│ + clear_all() → None                                        │
│ - _init_database() → None                                   │
│ - _hash_to_string(hash_token: Tuple) → str                  │
│ - _string_to_hash(hash_str: str) → Tuple                    │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                      API Router                             │
│                  (app/api/routes.py)                       │
├─────────────────────────────────────────────────────────────┤
│ - fingerprinter: AudioFingerprinter                          │
│ - db: PersistentDB                                           │
├─────────────────────────────────────────────────────────────┤
│ + init_routes(fingerprinter, db)                             │
│ + root() → Dict                                             │
│ + learn_song(file, song_name) → JSONResponse                │
│ + recognize_song(file) → JSONResponse                       │
│ + get_stats() → Dict                                        │
│ + list_songs() → Dict                                       │
│ + delete_song(song_name) → JSONResponse                     │
│ + clear_all_songs() → JSONResponse                          │
└─────────────────────────────────────────────────────────────┘
```

---

## 📊 Data Flow Diagram

```
                    ┌─────────────────┐
                    │  Audio File     │
                    │  (WAV/MP3/M4A) │
                    └────────┬────────┘
                             │
                             ▼
                    ┌─────────────────┐
                    │  Load Audio     │
                    │  - soundfile/   │
                    │    librosa      │
                    │  - Mono         │
                    │  - 22050 Hz     │
                    └────────┬────────┘
                             │
                             ▼
                    ┌─────────────────┐
                    │  Spectrogram    │
                    │  - STFT         │
                    │  - n_fft=4096   │
                    │  - hop=1024     │
                    └────────┬────────┘
                             │
                             ▼
                    ┌─────────────────┐
                    │  Find Peaks     │
                    │  - Max filter   │
                    │  - 20×20 window │
                    └────────┬────────┘
                             │
                             ▼
                    ┌─────────────────┐
                    │  Fingerprints   │
                    │  - Hash pairs   │
                    │  - (f1,f2,dt)   │
                    └────────┬────────┘
                             │
                    ┌────────┴────────┐
                    │                 │
                    ▼                 ▼
        ┌─────────────────┐  ┌─────────────────┐
        │  Learn Mode     │  │  Recognize Mode │
        │  - Store in DB  │  │  - Query DB     │
        └─────────────────┘  └─────────────────┘
```

---

## 🔐 Security & Error Handling

```
┌─────────────────────────────────────────────────────────────┐
│                    Request Flow                             │
│                                                              │
│  1. Client Request                                          │
│     │                                                        │
│     ├─> CORS Check ──────────────┐                          │
│     │                            │                          │
│     ├─> File Validation          │                          │
│     │   ├─> Content-Type check   │                          │
│     │   └─> Extension check      │                          │
│     │                            │                          │
│     ├─> Save Temp File           │                          │
│     │                            │                          │
│     ├─> Process (DSP/DB)         │                          │
│     │   ├─> Try/Except blocks    │                          │
│     │   └─> Error logging        │                          │
│     │                            │                          │
│     └─> Response                 │                          │
│         ├─> Success (200)        │                          │
│         ├─> Client Error (400)   │                          │
│         ├─> Not Found (404)      │                          │
│         └─> Server Error (500)   │                          │
│                                                              │
│  2. Cleanup                                                   │
│     └─> Delete temp file                                     │
└─────────────────────────────────────────────────────────────┘
```

---

## 📈 Performance Characteristics

```
┌─────────────────────────────────────────────────────────────┐
│              Performance Metrics                            │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  Audio Processing:                                           │
│  - Load audio:        O(n)        n = samples               │
│  - STFT:              O(n log n)                            │
│  - Peak finding:      O(f × t)    f=freq, t=time bins       │
│  - Fingerprint gen:   O(p²)       p = number of peaks       │
│                                                              │
│  Database Operations:                                        │
│  - Add song:          O(f)        f = fingerprints          │
│  - Query:             O(q × m)    q=query, m=avg matches    │
│  - Index lookup:      O(log n)    n = total fingerprints    │
│                                                              │
│  Typical Times (10s audio):                                  │
│  - Processing:        ~1-2 seconds                           │
│  - Database query:    ~0.5-1 second                         │
│  - Total:             ~2-3 seconds                           │
└─────────────────────────────────────────────────────────────┘
```



